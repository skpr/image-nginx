# The Wolfi base image does not ship the headers-more module, so the Server
# header cannot be removed entirely. "server_tokens off" (set in nginx.conf)
# hides the nginx version, leaving only "Server: nginx".
